# `03_shadow/`

Status: `[LOGIC TO BE CONFIRMED]`  
Purpose: **Non-authoritative experimentation** — curveballs, Kaizen probes, spike code, and “what if” work that must **not** be treated as production truth until explicitly promoted via `MANIFEST.md` and human-operator sign-off where required.

This folder **serves the clean-build goal** by **absorbing** material that would
otherwise bloat `00_authority/` or `01_truth/` — learning, diagnostics, and WIP
that still improve the *next* agent run when promoted or cited honestly.

- Never merge shadow assumptions into `00_authority/` or client-facing paths without the normal promotion path.

## Subfolders

These three subfolders are indexed in `00_authority/MANIFEST.md` under Candidate authority:

- `job-wrapups/` — wrap-up / escalation notes from completed agent sessions. See `job-wrapups/README.md` for naming and storage conventions.
- `strategies/` — synthesis / working-draft strategy documents (e.g., Ken-Huang-derived architecture chapters with Antigravity adaptations) that need GitHub permanence and an attribution chain but are not yet decisions. See `strategies/README.md` for the promotion path and the snapshot-vs-live model.
- `validators/` — shadow-tier landing zone for public-data verdicts produced by `02_build/validators/`. Non-authoritative pending human review and promotion to `01_truth/research/validations/`. See `validators/README.md`.

Other subfolders present on disk (`notes/`, `sessions/`) are not indexed in `MANIFEST.md` and are treated as transient working space.

Signed-by: Keystone (AI) — 2026-04-16  
Signed-by: Devon-5da3 | 2026-05-03 | devin-5da3bd275191469c8400142fd0ae1d69 (added Subfolders section listing all three MANIFEST-indexed subfolders)
